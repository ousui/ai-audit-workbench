# var

`var/` 存放工作台运行时产物，不随工作台提交。

## 是否提交

默认不提交。仅保留本 README 说明目录职责。

## 结构

```text
var/
  runs/   每次审计运行产物
  tmp/    临时文件
  cache/  可重建缓存，例如工具规则库和漏洞库缓存
  logs/   工作台运行日志
```

## 与 local 的区别

- `var/`：可清理、可重建或可从 run 复现。
- `local/`：包含个人长期状态或人工确认信息，不应随意删除。
